/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "standalone",
    // Optional: Add strict mode or other deployment configs here if needed
    reactStrictMode: true,
};

export default nextConfig;
